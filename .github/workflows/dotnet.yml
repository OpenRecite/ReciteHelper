name: .NET WPF Build

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v4
      with:
        submodules: recursive
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 10.0.x
    
    - name: Build
      run: dotnet build src/ReciteHelper.slnx --configuration Release

    - name: Publish Application
      run: |
        $projectFile = Get-ChildItem -Path . -Filter *.csproj -Recurse | Select-Object -First 1
        if ($projectFile) {
          dotnet publish $projectFile.FullName `
            --configuration Release `
            --output ./publish `
            --self-contained true `
            --runtime win-x64 `
            /p:PublishSingleFile=true
        } else {
          Write-Error "Could not find project file (.csproj)"
          exit 1
        }
    
    - name: Upload Artifacts
      uses: actions/upload-artifact@v4
      with:
        name: ReciteHelper-App
        path: ./publish/
