# 项目架构迁移文档

## 1. 迁移背景

本项目正在从传统的 MVC 架构迁移到领域驱动设计（DDD）架构，以提高代码的可维护性、可测试性和可扩展性。本次迁移主要涉及将核心业务逻辑工具类从 `src/Utils/` 文件夹迁移到 DDD 架构的 `ReciteHelper.Infrastructure/Services/` 文件夹，并创建相应的接口定义在 `ReciteHelper.Core/Interfaces/Services/` 文件夹中。

## 2. 迁移内容

### 2.1 接口创建（Core/Interfaces/Services/）

| 接口名称 | 功能描述 | 方法定义 | 文件路径 |
|---------|---------|---------|----------|
| ISimilarityCalculator | 计算文本相似度 | `double CalculateSimilarity(string text1, string text2)` | `ReciteHelper.Core/Interfaces/Services/ISimilarityCalculator.cs` |
| ITextExtractor | 提取文本内容 | `string ExtractText(string filePath)` | `ReciteHelper.Core/Interfaces/Services/ITextExtractor.cs` |
| IOcrService | 文字识别服务 | `string RecognizeText(string imagePath)` | `ReciteHelper.Core/Interfaces/Services/IOcrService.cs` |
| IParser | 数据解析和序列化 | `T Deserialize<T>(string json)`<br>`string Serialize(object obj)` | `ReciteHelper.Core/Interfaces/Services/IParser.cs` |

### 2.2 服务实现（Infrastructure/Services/）

| 服务名称 | 实现的接口 | 功能描述 | 文件路径 | 迁移来源 |
|---------|---------|---------|----------|----------|
| CosineSimilarityCalculator | ISimilarityCalculator | 实现余弦相似度计算 | `ReciteHelper.Infrastructure/Services/CosineSimilarityCalculator.cs` | `src/Utils/CosineSimilarity.cs` |
| TextExtractor | ITextExtractor | 实现文本提取功能 | `ReciteHelper.Infrastructure/Services/TextExtractor.cs` | `src/Utils/ExtractText.cs` |
| OcrService | IOcrService | 实现 OCR 文字识别 | `ReciteHelper.Infrastructure/Services/OcrService.cs` | `src/Utils/OCRProvider.cs` |
| ParserService | IParser | 实现数据解析和序列化 | `ReciteHelper.Infrastructure/Services/ParserService.cs` | `src/Utils/Parser.cs` |

### 2.3 依赖注入配置

在 `src/App.xaml.cs` 文件中添加了以下依赖注入配置：

```csharp
// 注册相似度计算服务
services.AddTransient<ISimilarityCalculator, CosineSimilarityCalculator>();

// 注册文本提取服务
services.AddTransient<ITextExtractor, TextExtractor>();

// 注册 OCR 服务
services.AddTransient<IOcrService, OcrService>();

// 注册解析服务
services.AddTransient<IParser, ParserService>();
```

### 2.4 编译问题修复

#### 2.4.1 扩展方法语法错误修复

修复了 `src/Utils/Extensions.cs` 文件中的扩展方法语法错误，将错误的 `extension` 关键字改为正确的 `this` 语法。

#### 2.4.2 Directory.Clear 方法调用修复

由于 `.NET` 中 `Directory` 类没有 `Clear` 方法，添加了 `DirectoryExtensions` 静态类和 `Clear` 静态方法，并修改了以下文件中的调用：

- `src/View/SelectChapterWindow.xaml.cs`：将 `Directory.Clear(path)` 改为 `DirectoryExtensions.Clear(path)`
- `src/MainWindow.xaml.cs`：将 `Directory.Clear(outputFolder)` 改为 `DirectoryExtensions.Clear(outputFolder)`

同时在这两个文件中添加了对 `ReciteHelper.Utils` 命名空间的引用。

## 3. 技术架构改进

### 3.1 依赖倒置原则

通过创建接口定义，实现了依赖倒置原则：
- 高层模块（如表示层）依赖于抽象（接口）
- 低层模块（如基础设施层）也依赖于相同的抽象
- 抽象不依赖于具体实现，具体实现依赖于抽象

### 3.2 可测试性

接口的创建使得代码更易于测试：
- 可以使用模拟对象（Mock）来测试依赖于这些服务的代码
- 可以在测试环境中替换具体实现，使用更简单的测试实现

### 3.3 可扩展性

架构的改进提高了代码的可扩展性：
- 可以轻松替换服务的实现，例如更换相似度计算算法
- 可以添加新的服务实现，而不影响现有代码

### 3.4 关注点分离

实现了更好的关注点分离：
- 核心层（Core）关注"做什么"（定义接口）
- 基础设施层（Infrastructure）关注"怎么做"（实现接口）
- 表示层（WPF）关注"如何展示"（使用服务）

## 4. 迁移效果

### 4.1 构建结果

项目构建成功，没有错误，只有一些依赖项版本兼容性的警告：
- Newtonsoft.Json 12.0.3 安全警告
- System.Drawing.Common 4.7.0 安全警告
- FuzzyString 1.0.0 版本兼容性警告
- Microsoft.SemanticKernel.Core 版本解析警告

### 4.2 运行状态

应用程序成功启动，所有功能正常运行。

## 5. 后续建议

1. **更新依赖项**：更新具有安全警告的依赖项到最新版本
2. **完善服务实现**：根据实际需求完善服务实现，特别是 OCR 和文本提取服务
3. **添加单元测试**：为迁移的服务添加单元测试，确保功能正确性
4. **文档更新**：更新项目文档，反映新的架构设计
5. **代码审查**：进行代码审查，确保代码质量和一致性

## 6. 总结

本次架构迁移成功将核心业务逻辑从 MVC 架构迁移到 DDD 架构，实现了更好的代码组织和架构设计。通过创建接口定义和服务实现，提高了代码的可维护性、可测试性和可扩展性，为后续的功能开发和代码维护奠定了良好的基础。